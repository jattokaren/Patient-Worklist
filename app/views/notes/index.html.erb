NOTES


<% @notes.each do |note| %>
  <br>
  <%= note.content %> <%= link_to("Note page", encounter_note_path(@encounter_id, note)) %>
  <br>
<% end %> 

<%= link_to("Create note", new_encounter_note_path(@encounter_id)) %>